# Tests

This folder is reserved for calculation logic and utility tests once tool implementations begin.
For now, it documents the intended boundary: calculation code should stay in `lib/` and be tested independently from UI components.
